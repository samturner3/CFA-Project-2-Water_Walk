<%= form_for(resource) do |f| %>
  <% if resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resource.errors.count, "error") %> prohibited this resource from being saved:</h2>

      <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_of %>
    <%= f.text_field :type_of %>
  </div>

  <div class="field">
    <%= f.label :stock %>
    <%= f.text_field :stock %>
  </div>

  <div class="field">
    <%= f.label :units %>
    <%= f.text_field :units %>
  </div>

  <% if params[:location] %>
  <input type="hidden" id="resource_location_id" name="resource[location_id]" value="<%= params[:location] %>" />
<% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
